# Optional — enables live OpenAI analysis (demo works without this)
OPENAI_API_KEY=

# Demo mode banner + bundled demo.db on Vercel
DEMO_MODE=true

# Local dev (default)
# DATABASE_URL=file:./dev.db
# PORT=4000
